Add screenshots here for the portfolio README.

Recommended captures:
1. Login page (with styled form)
2. File listing (with seeded demo files visible)
3. Upload in progress (showing HTMX spinner indicator)

To capture: run `docker compose up`, open http://localhost:8000, sign in with `secret123`.
