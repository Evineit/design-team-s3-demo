# Required
APP_PASSWORD=change-me-to-a-strong-password
S3_BUCKET=design-file-manager-dev
AWS_REGION=us-east-1

# Optional — only needed for local dev (not ECS)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
